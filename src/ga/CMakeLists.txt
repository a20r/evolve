CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET(LIBRARY_NAME evolve_ga)

# evolve_ga library
ADD_LIBRARY(
    ${LIBRARY_NAME}

    crossover.c
    mutation.c
)
TARGET_LINK_LIBRARIES(${LIBRARY_NAME} m)
TARGET_LINK_LIBRARIES(${LIBRARY_NAME} al)
TARGET_LINK_LIBRARIES(${LIBRARY_NAME} dstruct)
